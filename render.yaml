services:
  - type: web
    name: smart-dustbin-sim
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      # Set this if you want to use Neon/Drizzle in production
      # - key: DATABASE_URL
      #   sync: false
